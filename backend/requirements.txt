# Runtime dependencies for the Vercel Python (FastAPI) serverless function.
#
# Vercel installs these into the function; the local `prelegal_api` package is
# imported from `src/` by `index.py`, not pip-installed. Kept in sync with the
# runtime deps in `pyproject.toml`. `uvicorn` is intentionally omitted — Vercel's
# runtime serves the ASGI app, so no local server is needed (dropping it also
# trims uvloop/httptools/websockets from the bundle).
fastapi>=0.128.0
pydantic>=2.13.4
pydantic-settings>=2.12.0
libsql-experimental>=0.0.55
litellm>=1.92.0
